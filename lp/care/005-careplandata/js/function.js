//$(function(){
    //let headNav = $('#header');
    //loadしたとき
    //$(window).on('load',function(){
        //headerの高さ分上に設定（上から降りてくるようにするため）
        //headNav.css({top: '-60px'});
        //位置を0に設定、アニメーションでにゅるっと降りてくる
        //headNav.animate({top:0},1000);

        //main_textをゆっくり出現
       // $('.main_text,.main-cta').hide().fadeIn(1000);
    //});
//});